# 凭据（敏感，绝不提交）
data/creds/
user_*.json

# 对话录制产物
data/logs/
conversation_log*.jsonl
conversation_summary*.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# 本地运行时
.env
config.json
# 不全局忽略 *.lock——uv.lock 应跟踪（可重现构建）

# 账号运行时状态（每个账号一个 state.json，user_*.json 模式已覆盖）
data/state/

# 开发环境 / IDE / 工作流（不提交）
.claude/
.trellis/
CLAUDE.md
skills-lock.json
